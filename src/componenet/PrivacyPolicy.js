import React from 'react'
import Footer from './Footer'
import Header from './Header'

function PrivacyPolicy() {
  return (
    <>
      <Header />
      <div>
        <section className="hero-wrap hero-wrap-2" >
          <div className="overlay"></div>
          <div className="container">
            <div className="row no-gutters slider-text align-items-center justify-content-center">
              <div className="col-md-9 ftco-animate text-center">
                <h1 className="mb-2 bread">Privacy policy</h1>
                {/* <p className="breadcrumbs"><span className="mr-2"><a href="index.html">Home <i className="ion-ios-arrow-forward"></i></a></span> <span>Teacher <i className="ion-ios-arrow-forward"></i></span></p> */}
              </div>
            </div>
          </div>
        </section>
        <section>
          <div className='container Policy_text'>
            <h2>Heading For Policy</h2>
            <ul>
              <li>Footer frame and a left navigation frame. Left frame overlaps the footer frame. Enable/disable scrolling as required. This template is a "liquid layout", so it expands and contracts as you change your browser size.</li>
              <li>Footer frame and a left navigation frame. Left frame overlaps the footer frame. Enable/disable scrolling as required. This template is a "liquid layout", so it expands and contracts as you change your browser size.</li>
              <li>Footer frame and a left navigation frame. Left frame overlaps the footer frame. Enable/disable scrolling as required. This template is a "liquid layout", so it expands and contracts as you change your browser size.</li>
              <li>Footer frame and a left navigation frame. Left frame overlaps the footer frame. Enable/disable scrolling as required. This template is a "liquid layout", so it expands and contracts as you change your browser size.</li>
              <li>Footer frame and a left navigation frame. Left frame overlaps the footer frame. Enable/disable scrolling as required. This template is a "liquid layout", so it expands and contracts as you change your browser size.</li>
              <li>Footer frame and a left navigation frame. Left frame overlaps the footer frame. Enable/disable scrolling as required. This template is a "liquid layout", so it expands and contracts as you change your browser size.</li>
            </ul>
            <h2>Heading For Policy</h2>
            <ul>
              <li>Footer frame and a left navigation frame. Left frame overlaps the footer frame. Enable/disable scrolling as required. This template is a "liquid layout", so it expands and contracts as you change your browser size.</li>
              <li>Footer frame and a left navigation frame. Left frame overlaps the footer frame. Enable/disable scrolling as required. This template is a "liquid layout", so it expands and contracts as you change your browser size.</li>
              <li>Footer frame and a left navigation frame. Left frame overlaps the footer frame. Enable/disable scrolling as required. This template is a "liquid layout", so it expands and contracts as you change your browser size.</li>
              <li>Footer frame and a left navigation frame. Left frame overlaps the footer frame. Enable/disable scrolling as required. This template is a "liquid layout", so it expands and contracts as you change your browser size.</li>
              <li>Footer frame and a left navigation frame. Left frame overlaps the footer frame. Enable/disable scrolling as required. This template is a "liquid layout", so it expands and contracts as you change your browser size.</li>
              <li>Footer frame and a left navigation frame. Left frame overlaps the footer frame. Enable/disable scrolling as required. This template is a "liquid layout", so it expands and contracts as you change your browser size.</li>
            </ul>
            <h2>Heading For Policy</h2>
            <ul>
              <li>Footer frame and a left navigation frame. Left frame overlaps the footer frame. Enable/disable scrolling as required. This template is a "liquid layout", so it expands and contracts as you change your browser size.</li>
              <li>Footer frame and a left navigation frame. Left frame overlaps the footer frame. Enable/disable scrolling as required. This template is a "liquid layout", so it expands and contracts as you change your browser size.</li>
              <li>Footer frame and a left navigation frame. Left frame overlaps the footer frame. Enable/disable scrolling as required. This template is a "liquid layout", so it expands and contracts as you change your browser size.</li>
              <li>Footer frame and a left navigation frame. Left frame overlaps the footer frame. Enable/disable scrolling as required. This template is a "liquid layout", so it expands and contracts as you change your browser size.</li>
              <li>Footer frame and a left navigation frame. Left frame overlaps the footer frame. Enable/disable scrolling as required. This template is a "liquid layout", so it expands and contracts as you change your browser size.</li>
              <li>Footer frame and a left navigation frame. Left frame overlaps the footer frame. Enable/disable scrolling as required. This template is a "liquid layout", so it expands and contracts as you change your browser size.</li>
            </ul>
          </div>
        </section>
      </div>
      <Footer />
    </>
  )
}

export default PrivacyPolicy